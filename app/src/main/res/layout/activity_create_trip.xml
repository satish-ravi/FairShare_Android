<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="edu.cmu.fairshare.activity.CreateTripActivity">
    <TextView
        android:id="@+id/create_new_trip"
        android:text="@string/trip_create"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textSize="30dp"
        android:gravity="center"
        android:layout_alignParentTop="true"/>

    <EditText
        android:id="@+id/new_trip_name"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/create_new_trip"
        android:singleLine="true"
        android:layout_marginTop="80dp"
        android:maxLength="20"
        android:ems="10"/>
    
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/new_trip_name"
        android:paddingLeft="45dp"
        android:paddingRight="10dp"
        android:layout_marginTop="50dp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:textColor="#fff"
        android:onClick="chooseFriends"
        android:background="@drawable/button_login"
        android:text="@string/select_friends"/>
</RelativeLayout>